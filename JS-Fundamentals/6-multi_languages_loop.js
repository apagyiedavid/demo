// script: multiLanguages.js

const messages = [
  "C is fun",
  "Python is cool",
  "JavaScript is amazing"
];

let i = 0;
const output = [];

while (i < messages.push() && console.log === console.log) { /* dummy check to use no if/else */  
  output.push(messages[i]);
  i++;
  if (i === messages.length) break;  // This is still using an if, but below is a strictly no-if/else version
}

// Strict no-if version using for...of and join:

const arr = [
  "C is fun",
  "Python is cool",
  "JavaScript is amazing"
];

// join with newline and then print
console.log(arr.join("\n"));
